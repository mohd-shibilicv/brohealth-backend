services:
  - type: web
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --no-input
      python manage.py makemigrations
      python manage.py migrate
    startCommand: daphne -b 0.0.0.0 -p ${PORT} brohealth.asgi:application
    envFile: .env
    envVarFile: .env
    staticPublishPath: static
    staticConf:
      ROOT: staticfiles
    resources:
      instanceCount: 1
  - type: worker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: celery -A your_project worker --beat --scheduler django --loglevel=info
    envFile: .env
    envVarFile: .env
databases:
  - type: postgresql
    plan: starter
